import React from 'react'
//import ax

const Home = () => {
    // when the page loads send a requetst to api to fetch user flashcard category
    // display the categories as folders.
    // When the folders are clicked they will go their
    // respective folder page and display the flasscards there
    // this will have its own component/page jsx
    // display the categories using map

    // const url = apiUrlToFetchCategories
    const homeData = () => {
        // fetch the data from the api
        // try {
        //     const gresponse = await axios.get(url)
        //     displayData = gresponse.data
        // } catch (error) {
        //     console.log(error.response)
        // }
    }
    return (
        <section>
            <button>
                {/* (displayData.map(
                    (data) => {
                        <article> {data.name}</article>
                    }
            } */}
                ))
            </button>
        </section>
    )
}